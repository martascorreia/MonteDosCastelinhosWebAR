import React from 'react';
import "./InfoPage.css";

const InfoPage = (sondagemID) => {
  return (
    <div className="textButton">
      <button id="textBtn">{text.text}</button>
    </div>
  );
}

export default InfoPage;
